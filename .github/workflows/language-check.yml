name: Language Compliance Check

"on":
  pull_request:
    paths:
      - '**.md'
      - '**.yml'
      - '**.yaml'
      - 'docs/**'

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  language-check:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: '18'

      - name: 🔍 Check language compliance
        run: |
          chmod +x scripts/naming/language-checker.mjs
          node scripts/naming/language-checker.mjs .

      - name: 📊 Generate report
        if: always()
        run: |
          echo "## 🌐 語言合規性檢查報告" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ 語言檢查已執行" >> $GITHUB_STEP_SUMMARY
  
  # ==================== 互動式客服整合 ====================
  interactive-service:
    name: 🤖 Language Check 互動式客服
    needs: [language-check]
    if: always()
    uses: ./.github/workflows/interactive-ci-service.yml
    with:
      ci-name: "Language Compliance Check"
      ci-status: ${{ needs.language-check.result }}
      ci-context: |
        {
          "check_result": "${{ needs.language-check.result }}"
        }
      error-logs: ${{ needs.language-check.result == 'failure' && '語言合規性檢查失敗，請確保文檔遵循語言規範' || '' }}
    permissions:
      contents: read
      pull-requests: write
      issues: write
