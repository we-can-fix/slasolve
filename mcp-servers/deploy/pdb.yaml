apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mcp-servers-pdb
  labels:
    app: mcp-servers
    namespace.io/team: platform-team
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: mcp-servers
