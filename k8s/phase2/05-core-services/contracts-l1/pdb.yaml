# ============================================================================
# Contracts L1 Pod 中斷預算
# ============================================================================

apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: contracts-l1-pdb
  namespace: autofix
  labels:
    app: contracts-l1
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: contracts-l1
