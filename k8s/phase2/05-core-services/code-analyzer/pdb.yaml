# ============================================================================
# Code Analyzer Pod 中斷預算
# ============================================================================

apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: code-analyzer-pdb
  namespace: autofix
  labels:
    app: code-analyzer
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: code-analyzer
