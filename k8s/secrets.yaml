# ============================================================================
# Secrets Configuration Template
# ============================================================================
# NOTE: This is a template. Create actual secrets using:
# kubectl create secret generic autofix-bot-secrets \
#   --from-literal=db-password=<your-password> \
#   --from-literal=redis-password=<your-password> \
#   -n autofix-bot

apiVersion: v1
kind: Secret
metadata:
  name: autofix-bot-secrets
  namespace: autofix-bot
type: Opaque
stringData:
  # Database Credentials (CHANGE THESE IN PRODUCTION)
  db-username: "autofix_bot"
  db-password: "CHANGE_ME_IN_PRODUCTION"
  
  # Redis Password
  redis-password: "CHANGE_ME_IN_PRODUCTION"
  
  # API Keys (replace with actual keys)
  sonarqube-token: "CHANGE_ME"
  snyk-token: "CHANGE_ME"
  github-token: "CHANGE_ME"
  
  # Encryption Keys
  encryption-key: "CHANGE_ME_32_CHAR_HEX_STRING"
