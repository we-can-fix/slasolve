# Intelligent Automation Module Requirements
# Python dependencies for autonomous code analysis and fixing
#
# NOTE: This file defines OPTIONAL dependencies for the intelligent-automation module.
# The module uses a plugin architecture and can operate without external AI/ML dependencies.
# Python version requirement is managed by pyproject.toml or runtime environment.

# Async and Concurrency
typing-extensions>=4.11.0

# Configuration and Environment (optional)
python-dotenv>=1.0.0

# Logging (optional - uses standard logging if not available)
loguru>=0.7.2

# Development and Testing (dev-only)
pytest>=8.0.0
pytest-asyncio>=0.23.5
pytest-cov>=4.1.0

# Code Quality (dev-only)
black>=24.1.1
isort>=5.13.2
